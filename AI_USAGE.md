# AI_USAGE.md —— AI 辅助开发说明

> ⚠️ 本文件由我本人口述经历、AI 整理成稿,所有内容属实;提交前我已逐条核对并用自己的话改写。

## 使用的 AI 工具

| 工具 | 用途 |
|---|---|
| Claude Code(CLI,会话式编码助手) | 需求拆解与技术方案讨论;代码骨架起草;单测用例起草;**代码审查**(让它以批判视角重读关键文件);README/DESIGN 文档初稿 |
| (未使用任何线上 Agent / 批量生成器) | 本项目全程交互式人机协作,没有一键生成或无人值守跑批 |

## 哪些决策是我做的(人),哪些是 AI 建议后我采纳的

**我的决策(未采纳 AI 的不同意见):**

- 后端选 **Spring Boot 3**——AI 最初推荐 Node.js + Express(理由是题目允许且上手快),但我的技术栈是 Spring Boot,最终坚持 Java 方案
- 持久层选**原生 MyBatis(XML Mapper)**而非 AI 推荐的 JPA/MyBatis-Plus——我熟悉 XML 写法,SQL 全透明,面试讲解更清楚
- 游戏规则(限时积分制、+3/+1/0、90 秒弃赛宽限)、棋盘规格(8×8、8 图案 ×4 对)由我与 AI 讨论后拍板,并按演示效果调整过

**AI 提议、我评估后采纳的(举例):**

- 结算幂等采用「Redis SETNX 锁 → MySQL 唯一键兜底 → 修复路径」三层防线——我先质疑"唯一键是不是就够了",AI 画了崩溃窗口表(提交前崩/提交后崩/并发双触发),我逐格推演后认可
- 匹配配对用 Redis Lua 脚本保证原子性——AI 给出脚本,我对照 Redis 单线程模型确认并发安全
- 断线重连的值绑定 sessionId(防止旧断线事件误伤新连接)——AI 在一次审查中发现这个竞态,我复现场景后采纳
- 前端镜像一份路径校验算法仅做 UX 预判——我原本想抽共享包,AI 指出双端重复约 80 行更简单,我接受

## 提示词策略

- 先给上下文:把题目 PDF 全文、我的技术栈约束("只用 Spring Boot + MyBatis XML,不用 Spring Security 全量配置")一次性交代清楚
- 分步推进,每步先要"方案 + 理由 + 风险",再要代码;不直接要一大坨
- 关键算法(路径校验、结算幂等)要求"先给算法描述和伪代码,我确认后再写实现"
- 常让它做**反向审查**:"假设你是评审,找这个实现的漏洞",多次提前暴露问题

## 我亲自解决的一个问题(代表性案例)

**问题**:服务端实现 STOMP 心跳后,前端断线检测始终不生效——浏览器标签页休眠后连接早已断开,客户端却毫不知情。

**排查过程**:我先查前端配置(心跳参数 10s 已写)、再用浏览器 Network 面板确认客户端确实没有收到服务端 PING,定位到问题在服务端。阅读 Spring Boot 自动装配源码发现:`TaskSchedulingAutoConfiguration` 只有在容器里**不存在名为 `taskScheduler` 的 bean** 时才会创建它,而我在配置类里给 SimpleBroker 注入的是一个自定义 bean,导致容器里出现了两个 `TaskScheduler`——`@Scheduled` 定时任务和 STOMP 心跳各自绑到不同的调度器,心跳被静默吞掉。

**解决**:把自定义调度器 bean 改名为 `taskScheduler`,与自动装配约定对齐,心跳与 GameSweeper 共用同一个 4 线程池。这个问题 AI 最初没发现,是我顺着"前端正常、服务端无 PING"的二分定位读源码找到的;之后我让 AI 把这个坑写进 README 的坑位清单,避免回归。

## 对 AI 生成代码的审查与修改点(摘录)

1. **AuthChannelInterceptor 订阅即推快照有竞态**:AI 初版在 SUBSCRIBE 回调里立即推快照,客户端可能因订阅未生效而丢消息 → 改为延迟约 200ms 经 TaskScheduler 推送
2. **结算服务自调用失效**:AI 初版把 @Transactional 写在同类的私有方法上,Spring 代理不生效 → 拆出独立 SettlementPersistService bean
3. **匹配成功后只通知了后入队的玩家**:AI 初版漏了给先排队的一方发 match-found 通知 → 补双向通知(我在代码审查中发现)
4. **测试用例构造漏洞**:AI 初版"绕边应非法"的测试用例其实存在合法捷径(能 1 转弯连通),测试是"假绿" → 我封死测试棋盘结构后重写用例
5. **前端 TS 编译错误**:Vite 模板开启 `erasableSyntaxOnly` 后,AI 写的构造函数参数属性、enum 等语法被禁 → 逐个改为显式字段与字符串字面量联合类型
6. **`@Scheduled` 与心跳共用调度器后**,AI 建议加大线程池避免扫描阻塞心跳 → 池大小定为 4 并注释说明

## 声明

- 所有代码均经我逐文件阅读、理解,能向评审解释任何一行;测试与构建结果(unit 24 绿、前端 build 绿、服务端 test-compile 绿)为实际运行输出
- 演示录屏由我本人在本机真实操作录制,未剪辑拼接假流程
- 未在仓库中提交任何密钥/凭据;`.env` 已被 .gitignore 排除
